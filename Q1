#Q1) Complete the function that takes one argument, a list of words, and returns the length of the longest word in the list.
# For example:
# ['simple', 'is', 'better', 'than', 'complex'] ==> 7
# Do not modify the input list.

def Longest(x):
    max_len = len(a[0])
    max_word = a[0]
 
    for i in x:
        if(len(i) > max_len):
 
            max_len = len(i)
            max_word = i
 
    print("Longest Word :", max_word,)
    print("Length of the Word :", max_len,)
          
 
 
x = ['simple', 'is', 'better', 'than', 'complex']
Longest(x)
